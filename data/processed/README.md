Derived data should be written here.

- `sparse_votes/` — output from global fusion
- `scene_voxels/` — optional scene-level voxel caches
- `exports/` — method-specific NPZ roots
- `qc/` — overlays and geometry checks
